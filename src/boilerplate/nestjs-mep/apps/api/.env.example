# The "manage" env layer. Copy to apps/api/.env
# It only selects which resource file loads: loadEnv('api') reads apps/api/.env first,
# then apps/api/venv/.env.${NODE_ENV} (the actual config — see venv/.env.example).

NODE_ENV=dev
